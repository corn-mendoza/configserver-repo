eureka:
  client:
    shouldRegisterWithEureka: false
hystrix:
  stream:
    validate_certificates: false
fortuneService:
  scheme: http
  address: fortuneservice
  randomFortunePath: api/fortunes/random
  allFortunesPath: api/fortunes/all
ConnectionStrings:
    AttendeeContext: Server=pa-workshop.database.windows.net;Initial Catalog=AttendeeDB-S1;User ID=dbadmin;Password=PCF!Password;Connection Timeout=30;
